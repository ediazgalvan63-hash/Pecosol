RewriteEngine On
RewriteBase /

# Permitir directorios reales y archivos reales
RewriteCond %{REQUEST_FILENAME} -f [OR]
RewriteCond %{REQUEST_FILENAME} -d
RewriteRule ^ - [L]

# Reescribir todo lo demás a index.php
RewriteRule ^(.*)$ index.php [QSA,L]
